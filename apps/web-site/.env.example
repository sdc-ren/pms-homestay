# API domain — trang giá đọc GET /api/v1/public/plans lúc build/ISR.
NEXT_PUBLIC_API_URL=http://localhost:3001
# Nơi nút "Dùng thử" trỏ tới (web-admin).
NEXT_PUBLIC_APP_URL=http://localhost:3000
# Domain canonical của chính trang này (sitemap, OG).
NEXT_PUBLIC_SITE_URL=http://localhost:3004
